# 第五章：最佳实践与性能优化 - 生产级异步应用

> **学习目标**：掌握CompletableFuture的最佳实践，构建高性能、可维护的异步应用

---

## 一、线程池选择与配置

### 1.1 默认线程池的问题

```java
// 使用默认线程池
CompletableFuture.supplyAsync(() -> operation());
// 等价于
CompletableFuture.supplyAsync(() -> operation(), ForkJoinPool.commonPool());
```

**默认线程池的特点**：

```
线程数：CPU核心数 - 1

优点：
✅ 自动管理
✅ 适合CPU密集型任务

缺点：
❌ 所有CompletableFuture共享
❌ 不适合IO密集型任务
❌ 无法隔离不同业务
❌ 无法监控
❌ 线程名不友好（ForkJoinPool.commonPool-worker-1）
```

### 1.2 自定义线程池

```java
/**
 * 为不同类型的任务创建专用线程池
 */
public class ThreadPools {
    
    // IO密集型：数据库查询
    public static final ExecutorService DB_EXECUTOR = new ThreadPoolExecutor(
        20,                          // 核心线程数
        50,                          // 最大线程数
        60L,                         // 空闲线程存活时间
        TimeUnit.SECONDS,
        new LinkedBlockingQueue<>(200),
        new ThreadFactoryBuilder()
            .setNameFormat("db-pool-%d")
            .build(),
        new ThreadPoolExecutor.CallerRunsPolicy()
    );
    
    // IO密集型：HTTP请求
    public static final ExecutorService HTTP_EXECUTOR = new ThreadPoolExecutor(
        50,
        100,
        60L,
        TimeUnit.SECONDS,
        new LinkedBlockingQueue<>(500),
        new ThreadFactoryBuilder()
            .setNameFormat("http-pool-%d")
            .build(),
        new ThreadPoolExecutor.CallerRunsPolicy()
    );
    
    // CPU密集型：计算任务
    public static final ExecutorService CPU_EXECUTOR = new ThreadPoolExecutor(
        Runtime.getRuntime().availableProcessors(),
        Runtime.getRuntime().availableProcessors(),
        0L,
        TimeUnit.MILLISECONDS,
        new LinkedBlockingQueue<>(100),
        new ThreadFactoryBuilder()
            .setNameFormat("cpu-pool-%d")
            .build(),
        new ThreadPoolExecutor.AbortPolicy()
    );
}
```

**使用自定义线程池**：

```java
// 数据库查询
CompletableFuture.supplyAsync(() -> 
    userDao.getUser(userId), 
    ThreadPools.DB_EXECUTOR
);

// HTTP请求
CompletableFuture.supplyAsync(() -> 
    httpClient.get(url), 
    ThreadPools.HTTP_EXECUTOR
);

// CPU密集型计算
CompletableFuture.supplyAsync(() -> 
    complexCalculation(data), 
    ThreadPools.CPU_EXECUTOR
);
```

### 1.3 线程池大小计算

```java
/**
 * IO密集型任务
 * 线程数 = CPU核心数 * (1 + IO耗时/CPU耗时)
 */
int ioThreads = Runtime.getRuntime().availableProcessors() * 2;

/**
 * CPU密集型任务
 * 线程数 = CPU核心数 + 1
 */
int cpuThreads = Runtime.getRuntime().availableProcessors() + 1;

/**
 * 混合型任务
 * 需要根据实际情况测试调优
 */
```

---

## 二、避免常见陷阱

### 2.1 陷阱1：忘记指定线程池

```java
// ❌ 不好的做法：使用默认线程池
CompletableFuture.supplyAsync(() -> {
    // 数据库查询，IO密集型
    return userDao.getUser(userId);
});

// ✅ 好的做法：使用专用线程池
CompletableFuture.supplyAsync(() -> {
    return userDao.getUser(userId);
}, ThreadPools.DB_EXECUTOR);
```

### 2.2 陷阱2：阻塞操作

```java
// ❌ 不好的做法：在异步任务中阻塞
CompletableFuture.supplyAsync(() -> {
    try {
        Thread.sleep(1000); // 阻塞线程
    } catch (InterruptedException e) {
        e.printStackTrace();
    }
    return "result";
});

// ✅ 好的做法：使用异步API
CompletableFuture.supplyAsync(() -> "result")
    .thenApplyAsync(result -> {
        // 使用异步操作
        return asyncOperation(result);
    });
```

### 2.3 陷阱3：过度使用异步

```java
// ❌ 不好的做法：简单操作也异步
CompletableFuture.supplyAsync(() -> {
    return "hello".toUpperCase(); // 简单操作，不需要异步
});

// ✅ 好的做法：只对耗时操作异步
String result = "hello".toUpperCase(); // 直接执行

CompletableFuture.supplyAsync(() -> {
    return expensiveOperation(); // 耗时操作才异步
});
```

### 2.4 陷阱4：忘记处理异常

```java
// ❌ 不好的做法：不处理异常
CompletableFuture.supplyAsync(() -> riskyOperation());

// ✅ 好的做法：处理异常
CompletableFuture.supplyAsync(() -> riskyOperation())
    .exceptionally(ex -> {
        log.error("操作失败", ex);
        return defaultValue;
    });
```

### 2.5 陷阱5：内存泄漏

```java
// ❌ 不好的做法：持有大对象引用
List<byte[]> largeData = loadLargeData();
CompletableFuture.supplyAsync(() -> {
    // 闭包持有largeData引用
    return process(largeData);
});

// ✅ 好的做法：及时释放引用
List<byte[]> largeData = loadLargeData();
CompletableFuture<Result> future = CompletableFuture.supplyAsync(() -> {
    return process(largeData);
});
largeData = null; // 释放引用
```

---

## 三、性能优化技巧

### 3.1 批量操作优化

```java
// ❌ 不好的做法：串行处理
public List<User> getUsers(List<String> userIds) {
    return userIds.stream()
        .map(id -> userService.getUser(id)) // 串行
        .collect(Collectors.toList());
}

// ✅ 好的做法：并行处理
public CompletableFuture<List<User>> getUsersAsync(List<String> userIds) {
    List<CompletableFuture<User>> futures = userIds.stream()
        .map(id -> CompletableFuture.supplyAsync(
            () -> userService.getUser(id),
            ThreadPools.DB_EXECUTOR
        ))
        .collect(Collectors.toList());
    
    return CompletableFuture.allOf(futures.toArray(new CompletableFuture[0]))
        .thenApply(v -> futures.stream()
            .map(CompletableFuture::join)
            .collect(Collectors.toList())
        );
}
```

### 3.2 缓存优化

```java
/**
 * 带缓存的异步查询
 */
public class CachedAsyncService {
    private final LoadingCache<String, CompletableFuture<User>> cache = 
        CacheBuilder.newBuilder()
            .maximumSize(1000)
            .expireAfterWrite(10, TimeUnit.MINUTES)
            .build(new CacheLoader<String, CompletableFuture<User>>() {
                @Override
                public CompletableFuture<User> load(String userId) {
                    return CompletableFuture.supplyAsync(
                        () -> userService.getUser(userId),
                        ThreadPools.DB_EXECUTOR
                    );
                }
            });
    
    public CompletableFuture<User> getUser(String userId) {
        try {
            return cache.get(userId);
        } catch (ExecutionException e) {
            return CompletableFuture.failedFuture(e.getCause());
        }
    }
}
```

### 3.3 减少上下文切换

```java
// ❌ 不好的做法：频繁切换线程
CompletableFuture.supplyAsync(() -> step1(), executor1)
    .thenApplyAsync(r -> step2(r), executor2)
    .thenApplyAsync(r -> step3(r), executor3);

// ✅ 好的做法：在同一线程池中执行
CompletableFuture.supplyAsync(() -> step1(), executor)
    .thenApply(r -> step2(r)) // 同步执行，不切换线程
    .thenApply(r -> step3(r));
```

### 3.4 合理使用同步/异步方法

```java
// 简单转换：使用同步方法
CompletableFuture.supplyAsync(() -> getData())
    .thenApply(data -> data.toUpperCase()) // 同步，快速

// 耗时操作：使用异步方法
CompletableFuture.supplyAsync(() -> getData())
    .thenApplyAsync(data -> expensiveProcess(data)) // 异步，耗时
```

---

## 四、监控与诊断

### 4.1 添加日志

```java
public CompletableFuture<String> monitoredOperation() {
    long startTime = System.currentTimeMillis();
    String operationId = UUID.randomUUID().toString();
    
    log.info("开始操作: {}", operationId);
    
    return CompletableFuture.supplyAsync(() -> {
        log.info("执行操作: {}, 线程: {}", 
            operationId, Thread.currentThread().getName());
        return operation();
    })
    .whenComplete((result, ex) -> {
        long duration = System.currentTimeMillis() - startTime;
        if (ex != null) {
            log.error("操作失败: {}, 耗时: {}ms", operationId, duration, ex);
        } else {
            log.info("操作成功: {}, 耗时: {}ms", operationId, duration);
        }
    });
}
```

### 4.2 监控线程池

```java
/**
 * 可监控的线程池
 */
public class MonitoredThreadPool {
    private final ThreadPoolExecutor executor;
    
    public MonitoredThreadPool(ThreadPoolExecutor executor) {
        this.executor = executor;
        startMonitoring();
    }
    
    private void startMonitoring() {
        ScheduledExecutorService monitor = Executors.newScheduledThreadPool(1);
        monitor.scheduleAtFixedRate(() -> {
            log.info("线程池状态: " +
                "核心线程数={}, " +
                "活跃线程数={}, " +
                "队列大小={}, " +
                "完成任务数={}",
                executor.getCorePoolSize(),
                executor.getActiveCount(),
                executor.getQueue().size(),
                executor.getCompletedTaskCount()
            );
        }, 0, 30, TimeUnit.SECONDS);
    }
}
```

### 4.3 性能指标收集

```java
/**
 * 带指标收集的CompletableFuture
 */
public class MetricsCompletableFuture {
    private static final Counter successCounter = 
        Counter.build().name("async_success_total").help("成功次数").register();
    
    private static final Counter failureCounter = 
        Counter.build().name("async_failure_total").help("失败次数").register();
    
    private static final Histogram duration = 
        Histogram.build().name("async_duration_seconds").help("执行时间").register();
    
    public static <T> CompletableFuture<T> withMetrics(
            Supplier<T> supplier, Executor executor) {
        
        Histogram.Timer timer = duration.startTimer();
        
        return CompletableFuture.supplyAsync(supplier, executor)
            .whenComplete((result, ex) -> {
                timer.observeDuration();
                if (ex != null) {
                    failureCounter.inc();
                } else {
                    successCounter.inc();
                }
            });
    }
}
```

---

## 五、测试最佳实践

### 5.1 单元测试

```java
@Test
public void testAsyncOperation() throws Exception {
    // 使用测试线程池
    ExecutorService testExecutor = Executors.newFixedThreadPool(2);
    
    CompletableFuture<String> future = CompletableFuture
        .supplyAsync(() -> "result", testExecutor);
    
    // 等待结果
    String result = future.get(1, TimeUnit.SECONDS);
    
    assertEquals("result", result);
    
    testExecutor.shutdown();
}
```

### 5.2 测试异常处理

```java
@Test
public void testExceptionHandling() {
    CompletableFuture<String> future = CompletableFuture
        .supplyAsync(() -> {
            throw new RuntimeException("测试异常");
        })
        .exceptionally(ex -> "默认值");
    
    String result = future.join();
    assertEquals("默认值", result);
}
```

### 5.3 测试超时

```java
@Test(expected = TimeoutException.class)
public void testTimeout() throws Exception {
    CompletableFuture<String> future = CompletableFuture
        .supplyAsync(() -> {
            try {
                Thread.sleep(5000);
            } catch (InterruptedException e) {
                e.printStackTrace();
            }
            return "result";
        });
    
    // 1秒超时
    future.get(1, TimeUnit.SECONDS);
}
```

---

## 六、生产环境配置建议

### 6.1 线程池配置

```yaml
# application.yml
thread-pool:
  db:
    core-size: 20
    max-size: 50
    queue-capacity: 200
    keep-alive-seconds: 60
    thread-name-prefix: "db-pool-"
    
  http:
    core-size: 50
    max-size: 100
    queue-capacity: 500
    keep-alive-seconds: 60
    thread-name-prefix: "http-pool-"
    
  cpu:
    core-size: 8  # CPU核心数
    max-size: 8
    queue-capacity: 100
    keep-alive-seconds: 0
    thread-name-prefix: "cpu-pool-"
```

### 6.2 超时配置

```java
public class TimeoutConfig {
    // 数据库查询超时
    public static final long DB_TIMEOUT = 5000; // 5秒
    
    // HTTP请求超时
    public static final long HTTP_TIMEOUT = 3000; // 3秒
    
    // 复杂计算超时
    public static final long CALC_TIMEOUT = 10000; // 10秒
}
```

### 6.3 降级策略

```java
/**
 * 多级降级策略
 */
public CompletableFuture<Data> getDataWithFallback(String key) {
    return CompletableFuture
        // 1. 尝试从缓存获取
        .supplyAsync(() -> cache.get(key))
        .exceptionally(ex -> {
            log.warn("缓存失败，尝试主数据源");
            return null;
        })
        .thenCompose(cached -> {
            if (cached != null) {
                return CompletableFuture.completedFuture(cached);
            }
            
            // 2. 尝试从主数据源获取
            return CompletableFuture
                .supplyAsync(() -> primaryDataSource.get(key))
                .orTimeout(3, TimeUnit.SECONDS)
                .exceptionally(ex -> {
                    log.warn("主数据源失败，尝试备份数据源");
                    return null;
                });
        })
        .thenCompose(primary -> {
            if (primary != null) {
                return CompletableFuture.completedFuture(primary);
            }
            
            // 3. 尝试从备份数据源获取
            return CompletableFuture
                .supplyAsync(() -> backupDataSource.get(key))
                .orTimeout(5, TimeUnit.SECONDS)
                .exceptionally(ex -> {
                    log.error("所有数据源失败，返回默认值");
                    return Data.getDefault();
                });
        });
}
```

---

## 七、总结

### 7.1 最佳实践清单

```
线程池：
✅ 使用自定义线程池
✅ 为不同类型任务创建专用线程池
✅ 合理设置线程池大小
✅ 使用有意义的线程名

异常处理：
✅ 总是处理异常
✅ 设置超时
✅ 提供降级方案
✅ 记录日志

性能优化：
✅ 批量操作并行化
✅ 使用缓存
✅ 减少线程切换
✅ 合理使用同步/异步方法

监控：
✅ 添加日志
✅ 监控线程池状态
✅ 收集性能指标
✅ 设置告警

测试：
✅ 单元测试
✅ 异常测试
✅ 超时测试
✅ 压力测试
```

### 7.2 性能优化总结

```
1. 线程池优化
   - 专用线程池
   - 合理大小
   - 监控调优

2. 减少开销
   - 减少线程切换
   - 批量操作
   - 使用缓存

3. 资源管理
   - 及时释放引用
   - 设置超时
   - 限流降级
```

### 7.3 思考题

1. **如何选择线程池大小？**
2. **什么时候使用同步方法，什么时候使用异步方法？**
3. **如何监控CompletableFuture的性能？**
4. **如何实现多级降级策略？**

---

## 八、参考资料

- 《Java并发编程实战》
- 《Java性能优化权威指南》
- [CompletableFuture最佳实践](https://www.baeldung.com/java-completablefuture)
- [线程池调优指南](https://tech.meituan.com/2020/04/02/java-pooling-pratice-in-meituan.html)

---

**恭喜！你已经完成了CompletableFuture的深度学习！** 🎉

现在你可以：
- ✅ 理解异步编程的价值
- ✅ 熟练使用CompletableFuture
- ✅ 构建复杂的异步工作流
- ✅ 处理异常和超时
- ✅ 优化性能和监控
- ✅ 构建生产级异步应用
